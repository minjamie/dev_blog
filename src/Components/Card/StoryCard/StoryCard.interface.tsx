export interface StoryProp {
    data: {
        img: string;
        category: string;
        title: string;
        path: string;
        hasDoneInterview: boolean;
    };
    cardLength: number;
}
